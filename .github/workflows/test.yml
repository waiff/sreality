name: "CI: tests"

# CI test suite, runs on every push and pull request. Two parallel jobs:
# a Python job (verifies the filter-registry and workflow-docs codegen are
# fresh, then runs pytest) and a frontend job (TypeScript type-check + vitest
# pure-function tests). This is the build gate for every branch.

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install
        run: pip install -e ".[dev,api,geo]"

      # -m, not a script path: invoking by path puts scripts/ on sys.path[0] and
      # leaves the repo root off it, so the generator's `from toolkit import ...`
      # resolves through the ambient editable install rather than this checkout.
      # Benign on a clean CI runner, silently wrong on a dev machine whose
      # `pip install -e .` came from a git worktree — same command, different
      # tree. Keep the two invocations identical so local and CI agree.
      - name: Check filter registry codegen is fresh
        run: python -m scripts.generate_filter_registry --check

      - name: Check workflow docs codegen is fresh
        run: python scripts/generate_workflow_docs.py --check

      - name: Run pytest
        run: pytest -q

  vitest:
    # Frontend pure-function tests (vite/vitest, no jsdom yet).
    # Covers filter state plumbing, the registry adapter, and the
    # centre+radius spatial helpers in lib/queries.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        working-directory: frontend
        run: npm ci

      - name: Type-check
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Run vitest
        working-directory: frontend
        run: npm test
